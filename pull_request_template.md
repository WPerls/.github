## What and why

<One or two sentences. Link the finding id or the prompt if there is one.>

## Checks

- [ ] `npm test` passes locally (paste the summary line below)
- [ ] CI is green on this PR (GitHub Free cannot enforce it; look)
- [ ] Docs updated in this PR: CLAUDE.md, README.md, docs/ as needed
- [ ] `CHANGELOG.md` entry added under a new heading, time from `date`, newest first
- [ ] Vendored trees were re-vendored, not edited in place (`tests/vendor.test.js` green)
- [ ] `.env.example` matches every new env var read
- [ ] Both themes checked on every touched screen, or no UI in this change

## Needs

<Other PRs this one depends on, as `WPerls/<repo>#N`, or "none".>

## Acceptance output

```
<the command and its result>
```
