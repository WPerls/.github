# .github

Default community health files for every `WPerls` repo that has none of its
own. `templates/pull_request_template.md` is the PR body GitHub applies
automatically; see `willperlak.com/WORKSPACE.md`, "Before opening a PR".
